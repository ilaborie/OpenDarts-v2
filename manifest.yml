---
applications:
  dist/opendarts-2.0-SNAPSHOT.zip:
    name: opendarts
    framework:
      name: play
      info:
        mem: 256M
        description: Play Framework Application
        exec: 
    url: ${name}.${target-base}
    mem: 256M
    instances: 1
