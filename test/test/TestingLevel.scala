package test
import org.specs2.mutable._
import play.api.test._
import play.api.test.Helpers._
import dart._
import dart.Dart._
import ai._
import ai.x01._
import ai.x01.AiPlayerX01._
import test.x01.PlayX01

class TestingLevel extends Specification {
/*
	"Level 0" should {
		"play around 52 darts" in {
			val nbLegs = 100000
			val level = Level(0)
			// Launch
			val result = PlayX01.test501(nbLegs, level)

			print(level)
			print('\t')
			println(result)
			// Test
			result.avg must beCloseTo(52, 2.0)
		}
	}
	
	"Level 1" should {
		"play around 45 darts" in {
			val nbLegs = 100000
			val level = Level(1)
			// Launch
			val result = PlayX01.test501(nbLegs, level)

			print(level)
			print('\t')
			println(result)
			// Test
			result.avg must beCloseTo(45, 1.0)
		}
	}
	
	"Level 2" should {
		"play around 39 darts" in {
			val nbLegs = 100000
			val level = Level(2)
			// Launch
			val result = PlayX01.test501(nbLegs, level)

			print(level)
			print('\t')
			println(result)
			// Test
			result.avg must beCloseTo(39, 0.5)
		}
	}

	 "Level 3" should {
		"play around 36 darts" in {
			val nbLegs = 100000
			val level = Level(3)
			// Launch
			val result = PlayX01.test501(nbLegs, level)

			print(level)
			print('\t')
			println(result)
			// Test
			result.avg must beCloseTo(36, 0.5)
		}
	}


	"Level 4" should {
		"play around 33 darts" in {
			val nbLegs = 100000
			val level = Level(4)
			// Launch
			val result = PlayX01.test501(nbLegs, level)

			print(level)
			print('\t')
			println(result)
			// Test
			result.avg must beCloseTo(33, 0.5)
		}
	}

	"Level 5" should {
		"play around 30 darts" in {
			val nbLegs = 100000
			val level = Level(5)
			// Launch
			val result = PlayX01.test501(nbLegs, level)
			
			print(level)
			print('\t')
			println(result)
			// Test
			result.avg must beCloseTo(30, 0.5)
		}
	}

	"Level 6" should {
		"play around 28 darts" in {
			val nbLegs = 100000
			val level = Level(6)
			// Launch
			val result = PlayX01.test501(nbLegs, level)
			
			print(level)
			print('\t')
			println(result)
			// Test
			result.avg must beCloseTo(28, 0.5)
		}
	}

	"Level 7" should {
		"play around 26 darts" in {
			val nbLegs = 100000
			val level = Level(7)
			// Launch
			val result = PlayX01.test501(nbLegs, level)
			
			print(level)
			print('\t')
			println(result)
			// Test
			result.avg must beCloseTo(26, 0.5)
		}
	}

	"Level 8" should {
		"play around 24 darts" in {
			val nbLegs = 100000
			val level = Level(8)
			// Launch
			val result = PlayX01.test501(nbLegs, level)
			
			print(level)
			print('\t')
			println(result)
			// Test
			result.avg must beCloseTo(24, 0.5)
		}
	}

	"Level 9" should {
		"play around 22 darts" in {
			val nbLegs = 100000
			val level = Level(9)
			// Launch
			val result = PlayX01.test501(nbLegs, level)
			
			print(level)
			print('\t')
			println(result)
			// Test
			result.avg must beCloseTo(22, 0.5)
		}
	}

	"Level 10" should {
		"play around 20.5 darts" in {
			val nbLegs = 100000
			val level = Level(10)
			// Launch
			val result = PlayX01.test501(nbLegs, level)
			
			print(level)
			print('\t')
			println(result)
			// Test
			result.avg must beCloseTo(20.5, 0.5)
		}
	}

	"Level 11" should {
		"play around 19 darts" in {
			val nbLegs = 100000
			val level = Level(11)
			// Launch
			val result = PlayX01.test501(nbLegs, level)
			
			print(level)
			print('\t')
			println(result)
			// Test
			result.avg must beCloseTo(19, 0.5)
		}
	}

	"Level 12" should {
		"play around 17.5 darts" in {
			val nbLegs = 100000
			val level = Level(12)
			// Launch
			val result = PlayX01.test501(nbLegs, level)
			
			print(level)
			print('\t')
			println(result)
			// Test
			result.avg must beCloseTo(17.5, 0.5)
		}
	}
	
	"Level 13" should {
		"play around 16 darts" in {
			val nbLegs = 100000
			val level = Level(13)
			// Launch
			val result = PlayX01.test501(nbLegs, level)
			
			print(level)
			print('\t')
			println(result)
			// Test
			result.avg must beCloseTo(16, 0.5)
		}
	}
 	
	"Level 14" should {
		"play around 14.5 darts" in {
			val nbLegs = 100000
			val level = Level(14)
			// Launch
			val result = PlayX01.test501(nbLegs, level)
			
			print(level)
			print('\t')
			println(result)
			// Test
			result.avg must beCloseTo(14.5, 0.5)
		}
	}
	
	"Level 15" should {
		"play around 13 darts" in {
			val nbLegs = 100000
			val level = Level(15)
			// Launch
			val result = PlayX01.test501(nbLegs, level)
			
			print(level)
			print('\t')
			println(result)
			// Test
			result.avg must beCloseTo(13, 0.5)
		}
	}
	*/
}